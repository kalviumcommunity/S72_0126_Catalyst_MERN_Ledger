# ===================================================================
# ENVIRONMENT VARIABLES TEMPLATE
# ===================================================================
# This file is SAFE to commit to git
# It contains NO actual secrets, only variable names and descriptions
# Developers should copy this to create their environment-specific files
#
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env.staging (for staging environment)
# 2. Copy this file to .env.production (for production environment)
# 3. Copy this file to .env.development (for local development)
# 4. Fill in actual values in each file (NEVER commit those files)
# ===================================================================

# -------------------------------------------------------------------
# APPLICATION ENVIRONMENT
# -------------------------------------------------------------------
# Defines which environment the app is running in
# CRITICAL: This determines which credential set is active
# Values: development | staging | production
NODE_ENV=

# -------------------------------------------------------------------
# DATABASE CONNECTION
# -------------------------------------------------------------------
# PostgreSQL/MySQL/MongoDB connection string
# CRITICAL: This MUST be different for each environment
# Staging should NEVER use the production database URL
#
# Example formats (DO NOT use these actual values):
# PostgreSQL: postgresql://user:password@host:5432/dbname
# MongoDB: mongodb+srv://user:password@cluster.mongodb.net/dbname
# MySQL: mysql://user:password@host:3306/dbname
DATABASE_URL=

# -------------------------------------------------------------------
# API KEYS & EXTERNAL SERVICES
# -------------------------------------------------------------------
# Third-party API key (Stripe, SendGrid, AWS, etc.)
# CRITICAL: Use TEST/SANDBOX keys for staging
# Use LIVE/PRODUCTION keys only for production
# This prevents staging transactions from affecting real customers
API_KEY=

# API Secret for authentication
# CRITICAL: Each environment must have unique secrets
# Rotating secrets per environment limits breach impact
API_SECRET=

# -------------------------------------------------------------------
# AUTHENTICATION & SESSION
# -------------------------------------------------------------------
# Secret key for signing JWT tokens or sessions
# CRITICAL: Must be cryptographically random (use: openssl rand -base64 32)
# Each environment MUST have a different secret
# If staging secret leaks, production sessions remain secure
JWT_SECRET=

# Next.js NextAuth secret (if using NextAuth.js)
# CRITICAL: Different secret per environment
NEXTAUTH_SECRET=

# -------------------------------------------------------------------
# APPLICATION URL
# -------------------------------------------------------------------
# The public URL where the application is accessible
# Used for OAuth callbacks, email links, CORS configuration
# CRITICAL: Must match the actual deployment URL
# Staging: https://staging.yourapp.com
# Production: https://yourapp.com
NEXTAUTH_URL=

# -------------------------------------------------------------------
# OPTIONAL: MONITORING & LOGGING
# -------------------------------------------------------------------
# Sentry DSN for error tracking
# TIP: Use different Sentry projects for staging vs production
# This keeps error logs separated and easier to debug
SENTRY_DSN=

# -------------------------------------------------------------------
# SECURITY CHECKLIST BEFORE DEPLOYMENT:
# -------------------------------------------------------------------
# ✓ All DATABASE_URL values point to different databases
# ✓ All API_KEY values use appropriate environment keys (test vs live)
# ✓ All JWT_SECRET values are unique and cryptographically random
# ✓ All NEXTAUTH_URL values match the deployment URLs
# ✓ No .env.staging or .env.production files are committed to git
# ✓ CI/CD secrets are configured in GitHub/GitLab for each environment
